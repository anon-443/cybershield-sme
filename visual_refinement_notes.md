# Visual Refinement Notes

- The supplied 499×110 mobile-header crop shows the visible navigation links positioned across the header rather than centered as a compact group.
- The supplied 1231×126 assessment crop shows a small dark partial circular/rounded artifact protruding below the lower-left edge of the assessment card. It should be removed without changing the assessment card or primary sign-in/assessment button position.

The 499-pixel development drawer capture now shows all mobile navigation labels centered within the compact drawer while the close control remains available in the header. The 1231-pixel assessment capture shows the complete white assessment card with its primary button and lower-left edge clean after the clipped privacy row was removed; the assessment console itself has not moved.

The final recheck confirmed the drawer's centered navigation group at 499 pixels and the clean lower-left assessment-card edge at 1231 pixels. The centered desktop navigation group remains visually balanced independently of the mobile drawer.

The latest 1339-pixel signed-out preview confirms that the primary navigation is in the upper-right corner and that Sign in is a distinct filled action. The 499-pixel mobile check confirms the compact brand-and-menu header remains intact.

The GitHub Pages URL was checked immediately after updating the `gh-pages` branch, but it was still serving the previous static showcase during deployment propagation. The updated branch commit is `c5f013b002ef4c77fb479b9983e5c24659adfac7` and requires a subsequent public verification after GitHub Pages finishes rebuilding.

The high-fidelity GitHub Pages mirror was visually checked at 1440×1100 and 390×844 after commit `570f7346b731116ee8890bd31b09f9449b511009`. The desktop capture shows the matching corner header and prominent sign-in control, compact left radar visual, two-row hero, full assessment console, proof rail, and method ledger. The mobile capture shows the compact brand/menu header, readable stacked hero, full radar panel, assessment console, and responsive proof content. Both captures preserve the live landing page's pale-green visual system, sizing hierarchy, and formatting.

Full-page desktop QA tiles 1–2 confirm the complete first-screen composition, active/hover-capable right-corner navigation, assessment console sizing, proof rail, method-ledger continuation, three coverage cards, and the start of the safe-boundary section. The vertical overlap between the two tiles preserves the method-to-coverage transition without a visible gap.

Desktop tiles 3–4 confirm the complete safe-boundary shield, responsibility statements, three safety points, and the footer. Tile 4 is intentionally blank because the fixed 3,800-pixel capture height extends beyond the rendered document; no page content is missing between the boundary/footer and the blank region.

Mobile full-page tiles 1–2 confirm the compact header and menu trigger, readable two-line headline, correctly sized actions, full radar visual, entire assessment console, and the four proof-rail items in a two-column responsive arrangement. The shared overlap shows the radar-to-console boundary without a gap or overlap.

Mobile tiles 3–4 confirm the complete method heading, explanatory paragraph, all three ledger entries, and a clean visual transition into the pale-green coverage section. The long coverage heading remains readable at the narrow width without horizontal overflow.

Mobile tiles 5–6 confirm all three coverage cards stack cleanly with consistent icon, label, heading, description, and weight formatting. The following safe-by-design shield begins with the correct centered scale and no spacing discontinuity.

Mobile tiles 7–8 confirm the full safety-boundary heading, explanation, three no-intrusion safeguards, and footer. Tile 8 is blank after the footer because the fixed 6,000-pixel capture extends beyond the mobile document height; the footer in tile 7 is the end of page content.

Mobile tiles 9–10 contain only the expected blank continuation of the fixed-height capture after the already-verified footer, confirming no hidden or clipped page content remains below the document end.

Mobile tile 11 was also reviewed in order and contains the same expected blank capture continuation after the complete page footer.

Authenticated workspace readability QA was inspected at 1440×1000 and 390×844. The report view now shows high-contrast domain, assessment label, responsible-use line, comparison heading, baseline explanation, and public-context values. The dashboard view now shows heavier title and supporting copy, readable summary cards, score-trace message, ledger rows, and action button. At the compact width, the same labels, action, card values, comparison summary, and score-trace copy remain readable without thin low-contrast text. Sidebar selectors and the print-action treatment were strengthened in the same workspace-only pass; the public landing-page palette and type remain untouched.

Direct desktop screenshot review confirms that the report's domain title, assessment status, comparison card, evidence values, and outlines now read sharply against the pale background. The dashboard's workspace title, supporting sentence, assessment card, trend prompt, saved-ledger labels, score, and action control are similarly legible with strong contrast and weight.

Direct compact-width screenshot review confirms that the report hero, grade, responsible-use statement, baseline card, and explanatory copy remain crisp without clipping or thin low-contrast text. The mobile dashboard preserves a strong heading, supporting copy, prominent assessment action, readable latest-assessment card, score-trace card, and section label.

Downloaded-report audit note: the attached exported file `CyberShieldSME—SignalObservatory.pdf` renders as a multi-page report with six pages. The export appears to include the report hero, category scoring, public-context content, AI guidance, and the evidence ledger rather than truncating after the first screen. Further page-by-page inspection is required to confirm whether any print spacing or margin issue remains, especially relative to the large left-side empty area seen in the browser screenshot, which currently appears to be the persistent authenticated sidebar rather than missing PDF content.

Downloaded PDF visual audit completed: the six-page export begins with the assessment summary and score/comparison content, continues with category cards and public context, presents the AI guide in readable three-card form, and ends through the evidence ledger with the report scope notes. Its print layout intentionally omits the application sidebar, so the wide browser-side navigation area is not carried into the PDF.

Real saved-report route verification completed with `velodrive-rentals.me`. At desktop width, the report automatically uses a narrow left icon rail: the collapse control, assessment icon, history icon, and user initial remain visible while the content begins much farther left and gains substantial reading width. At 390px, the desktop rail is replaced by the normal compact mobile header; the report heading, grade, print button, safety statement, comparison panel, and categories remain readable without clipping.

Direct screenshot inspection confirms the desktop rail is only a slim icon column rather than the previous wide, mostly empty navigation panel. Its toggle control, two navigation icons, and avatar initial remain visible, while the report card begins near the rail and spans the page. The inspected mobile rendering instead shows the normal CyberShield header, Scan history return link, and fully visible Print / Save PDF button above the readable report summary.

Report-controls QA: desktop saved reports show the compact rail’s low-light moon control alongside the new Share Report and Print / Save PDF actions. Mobile initially revealed the additional action could clip the PDF control; the action bar was corrected into a two-row grid. Direct reinspection at 390px confirms Scan history occupies the first row and Share Report plus Print / Save PDF are fully visible side by side on the second row. The compact mobile header retains the low-light control.

Low-light QA: direct desktop and mobile report captures confirm the report-only dark treatment preserves high-contrast title, assessment timestamp, grade, safety statement, comparison panel, score cards, rail icons, Share Report action, and PDF control. The moon control changes to a sun in the low-light state. The report-share helper is covered for successful native sharing, user cancellation, private-link clipboard fallback, and blocked-copy feedback; it never makes a report public or sends anything automatically.

Direct screenshot inspection shows the dark control’s active state in the compact desktop rail (sun icon) and in the mobile header (sun icon with warm low-light treatment). Both captures show crisp white titles and descriptions, clear mint buttons, and readable green-bordered cards. The Share Report and Print / Save PDF buttons are fully visible; on mobile they remain aligned side by side directly beneath the Scan history return action.

Comprehensive mobile contrast QA, tiles 1–2: the header, low-light control, Scan history return, Share Report, and Print / Save PDF are all fully visible. The assessment title, timestamp, grade, authorization notice, comparison heading, baseline message, score labels, zero-value deltas, points labels, and previous-scan note use dark, readable text with clearly separated pale panels.

Comprehensive mobile contrast QA, tiles 3–4: category card names, grade letters, numeric scores, score denominators, progress captions, public-context heading, passive-data badge, TLS/certificate/registration labels, field values, and availability notes all remain legible with clearly visible card boundaries.

Comprehensive mobile contrast QA, tiles 5–6: the AI-assisted guide, overview, priority labels, action titles, Why it matters labels, business-impact copy, Next step labels, and remediation copy are all dark and readable inside the lavender guide surface and white action cards.

Comprehensive mobile contrast QA, tiles 7–8: the AI disclaimer, Evidence Ledger eyebrow, Observed findings title, explanatory copy, category filters, selected filter state, status icons, finding titles, category/severity metadata, colored severity chips, and chevrons all have clear contrast and readable sizing.

Comprehensive mobile contrast QA, tiles 9–10: all remaining evidence titles, categories, severity chips, status icons, and chevrons retain clear contrast. The final Scope boundary and Interpretation notes have dark bold labels, readable explanatory text, visible safety icons, and clearly defined card borders. No faint lower-page text remains in the inspected report.

Direct full desktop report inspection confirms that the strengthened text treatment carries through the complete wide layout: all category scores, context fields, AI guide cards, evidence-filter chips, every finding title/metadata/severity chip, and the paired scope/interpretation notes are dark and readable against the pale workspace. The compact rail remains narrow and does not consume report reading space.

Comprehensive desktop contrast QA, tiles 1–2: the report actions, assessment hero, authorization notice, comparison labels and metrics, score-card values, public-context labels/values, AI guide heading/badge/overview, priority labels, Why it matters copy, Next step copy, and disclaimer all render in strong dark text with clear borders and adequate sizing.

Comprehensive desktop contrast QA, tiles 3–4: the Evidence Ledger heading, category filters, finding titles, category/severity metadata, status icons, Information/High/Medium/Low chips, chevrons, and all remaining findings are readable at desktop scale. The two final safety notes use dark bold headings and high-contrast explanatory text; overlap between tiles 3 and 4 is consistent, and no content is omitted.

Verified release checkpoint saved: `aaca5a15` — Comprehensive report readability release. The checkpoint follows the direct desktop and mobile inspections and the passing 29-test validation suite.

Certificate-transparency clarification QA, desktop: the saved report’s Public Context card now shows `Unavailable` for certificate-listed names when the lookup was unavailable, followed by `Public certificate transparency lookup was unavailable; no conclusion about listed names.` The message wraps within the context card without collision; the adjacent registration/TLS fields, guide, and evidence ledger remain aligned and readable.

Certificate-transparency clarification QA, mobile: the same `Unavailable` status and explanatory message fit inside the narrow context card with intentional line wrapping and no horizontal overflow. The updated card remains distinct from registration-age context and does not disrupt the report’s score cards, guidance cards, ledger filters, or final scope notes.

Report actions QA, desktop: the top action bar presents Scan history, Rerun scan, Share Report, and Print / Save PDF with clear spacing. The Evidence Ledger adds a distinct Export report PDF control beside the category filters. The current informational Git metadata row remains neutral, confirming that the red exposure treatment is not applied without a confirmed failing record.

Report actions QA, mobile: Scan history stays on its own first row, with Rerun scan, Share Report, and Print / Save PDF fully visible in the compact three-column action row. The ledger’s Export report PDF control spans its own readable row above the responsive category filters; no text or control is clipped.

Confirmed Git-exposure preview QA: desktop verification shows a red left rule, red failure icon, red warning triangle, rose background, and Critical badge only for a recorded Git metadata failure whose saved evidence contains HTTP 200. The compact preview uses the same hierarchy; the long finding title is configured to wrap rather than be truncated at the narrow width.

Confirmed Git-exposure recheck, mobile: `Public Git metadata exposure` now wraps across two lines beside the red warning triangle. The Critical badge and chevron remain fully visible, confirming the narrow confirmed-exposure row contains no clipped text.

Motion-system QA, desktop: the landing page retains its compact hero, complete assessment console, proof rail, method ledger, coverage cards, safety boundary, and footer with no overlap or visible jump in the static rendered state. The staged entrance and radar/scan energy use transform-and-opacity animation, while the card-depth treatment is constrained to interactive lift and does not change the page palette or reading hierarchy.

Motion-system QA, mobile: the compact header, hero, radar, full assessment console, proof rail, method content, stacked coverage cards, and safety boundary remain readable and free of horizontal overflow. Pointer-driven transforms are optional enhancements; the reduced-motion rule resets transforms and removes non-essential transitions for users who request reduced motion.

Coverage metric-tooltip QA, full desktop capture: each coverage card now includes a compact View metrics control without affecting the card title, description, weight, or responsive grid. The full-page capture intentionally omits the floating tooltip overlay, so a dedicated viewport preview is used to inspect the open overlay itself.

Coverage metric-tooltip QA, direct overlay previews: the Website surface tooltip shows four concrete passive metrics, check icons, and the public-passive boundary note with high contrast at both desktop and mobile widths. The content fits within the mobile viewport without truncation; collision padding is reserved so the live hover/focus panel retains a comfortable edge margin.

Global dark-mode QA, desktop landing: the persistent control is visible beside the desktop navigation, with the full hero, animated radar visual, assessment console, proof rail, method ledger, and all text retaining readable contrast on the charcoal/emerald night palette. The saved report’s site-wide dark treatment retains high-contrast hero, score, comparison, and action controls without affecting the passive-only safety copy.

Evidence Ledger filter QA, desktop: the light report now presents an Export PDF action, keyword search, category chips, result chips, severity chips, and a live visible-count summary directly above the finding rows. The full-page render shows all 17 recorded findings before filtering and retains readable row labels, badges, and closing scope notes.

Dashboard comparison QA, desktop: the dashboard’s top summary now includes a distinct Latest vs previous panel alongside the latest score and trend. It shows overall, website, and email deltas plus an Open comparison details action; the same-domain historical rows remain visible beneath it.

Global dark-mode and comparison QA, mobile: the night-mode control remains reachable in the compact landing header; the hero, radar, assessment console, proof rail, coverage cards, and safety boundary stack without horizontal overflow. The mobile dashboard comparison panel keeps all three deltas and its Open comparison details action readable above the saved assessment ledger.

Evidence Ledger filter QA, mobile: the PDF export, full-width keyword search, stacked Category/Result/Severity chip groups, and 17-of-17 count remain readable above the finding rows. Every chip row wraps intentionally inside the narrow report; finding badges, titles, and final scope notes remain visible without horizontal clipping.

Assessment-console signal animation QA, desktop: the public-domain console now shows a visible signal-channel readout, animated status beacon, staged secure-review cipher line, and moving scan-light field inside the card. The effect sits behind the form and leaves the domain input, authorization control, and primary action fully readable.

Assessment-console signal animation QA, mobile: the signal-channel readout intentionally splits to a second monospace line within the narrow console. The console retains readable title, public-only label, domain input, and optional-email disclosure without horizontal clipping or overlay interference.

Particle-reveal and console-edge QA, desktop: the complete green Run secure assessment button, including its bottom radius, is now visible within the assessment card. The small teal particles and scan-light field remain decorative behind the readout, input, authorization control, and CTA rather than masking text.

Particle-reveal QA, mobile: the particle field remains subtle behind the compact console with no overlap on the signal-channel readout, domain field, helper copy, or disclosure. The primary action remains in normal mobile flow below the form controls.

First-screen console and controls QA, wide desktop: the full assessment console now begins directly beneath the action row and its complete Run secure assessment button is visible inside the first viewport. The top-right Particles on control is readable, the open hover tooltip stays above the CTA without blocking it, and the loading preview shows the rotating spinner plus Running secure assessment label within the expanded progress card.

Console controls QA, mobile: the compact console keeps its Particles on setting beside the title, while the normal form retains the full consent line and Run secure assessment action. In the mobile loading preview, the same console smoothly replaces the form with stage progress and an in-card spinner action; surrounding proof, method, coverage, and scope content remains cleanly stacked.

Requested console-position QA: at the supplied 1916px-wide desktop viewport, the complete assessment console is now lower—directly beneath the hero actions in the reference-aligned position—while its full consent row and green primary action remain inside the first screen. The desktop-only adjustment leaves the compact mobile console unchanged, where the title, Particles control, signal readout, and domain field remain readable.

Hero-gap correction QA: the wide desktop console now sits immediately beneath the hero action row, eliminating the conspicuous blank band above its top edge. The full card—including its particle toggle, domain field, optional-detail control, authorization row, and green primary action—remains visible. The mobile layout is unchanged and retains its clean stacked console flow.

Final measured first-screen correction: at the user’s 1271×711 laptop viewport, the assessment card begins immediately beneath the hero action row with a small intentional separation, not a blank band. The entire card—through the green Run secure assessment action—fits before the fold. The same desktop-only adjustment leaves the mobile header, hero, radar, and stacked assessment card clean and readable.

Domain-input usability QA, desktop: valid input displays the subtle hover-ready treatment and a compact Clear control. Invalid `example.com/admin` immediately receives a red border, red globe cue, and clear local-format explanation while preserving the field’s full width and the public-only marker.

Domain-input usability QA, mobile: the invalid state, explanatory error, Clear control, and public-only marker fit on the same compact input row without collision. The valid state uses the normal high-contrast helper copy and the inline Clear control remains visible.
Default-state follow-up: at 1271×711 and 390×844, an empty domain field has no error treatment or Clear control. The normal helper copy stays legible in the persisted night theme, and the desktop console still fits its full action within the first screen.
Assessment usability update QA, desktop: the requested particle-control icon is removed from the console header. At 1271×711, the headline and console sit higher while retaining the compact left radar, full authorization form, and primary action within the first screen. The development-only progress preview shows the live stage name, 84 percent value, advancing bar, completed stage markers, and a rotating progress cue.
Assessment usability update QA, mobile: the compact header, full radar visual, and console remain readable at 390×844. The private recent-domain panel opens below the input without horizontal overflow and labels suggestions as prefill-only; it explicitly says a selection cannot start an assessment. The private report view shows Rerun scan, Share Report, Download CSV, and Print / Save PDF as separate visible controls without clipping.
Saved-report refinement QA, desktop: the compact icon rail now uses grouped mint-green accents and subtle separator guides rather than a blank white column. The action bar differentiates navigation, rerun, sharing, CSV, and primary PDF export with restrained semantic color while keeping each label legible. The report summary is noticeably denser, and the score orb uses a grade-aware accent surface.
Saved-report refinement QA, mobile: the compact action panel keeps Scan history on the first line and the Rerun, Share, CSV, and PDF actions visible as distinct colored controls below it. The shortened summary card retains a readable domain, timestamp, grade, and score with no clipping or horizontal overflow.
Icon-rail color QA, desktop light theme: the report rail now visibly differentiates the navigation control in blue, the assessment icon in emerald, the history icon in violet, the theme control in amber, and the user marker in cyan. These compact color tiles are legible and keep the action bar’s green, violet, amber, and mint hierarchy coherent.
Icon-rail color QA, mobile: the rail correctly collapses into the compact header without any overflow, while the report’s colored action panel and grade orb remain visible and readable. The desktop-only color accents do not crowd the mobile report header.
Normal-scale baseline, 1918×1062: the landing page retains a clean compact hero and first-screen console, but its content width and visual scale remain deliberately restrained from the earlier zoom-driven sizing pass. The authenticated dashboard shows the same issue more clearly: its cards and ledger sit inside a narrow centered workspace with large unused side margins at this normal display width.
Normal-scale correction, 1918×1062: the landing headline now remains completely visible as a deliberate two-line title above the assessment console, while the radar, full authorization form, and proof rail scale proportionally. The dashboard uses more of the available width with readable headline, comparison cards, history rows, and action control. The saved report now uses a wider content surface with large readable action controls, domain title, grade, comparison panel, category scores, and colored icon rail.
Normal-scale mobile follow-up, 390×844: the landing page keeps its complete visible title, responsive radar, and vertical console without horizontal overflow. The dashboard preserves readable cards and assessment action. The saved report keeps all colored actions, summary, score, and authorization statement visible; controls wrap intentionally rather than clipping.
First-screen density correction, 1916×1061: the radar visual and assessment console are visibly larger, the proof rail begins immediately beneath the console, and the next content section follows without a blank band. The complete authorization form and Run secure assessment action remain visible before the proof rail.
First-screen density correction, 390×844: the wide-screen scale rules do not affect the mobile layout; its title, actions, radar visual, and stacked assessment form remain readable and do not overlap or overflow.
Hero equal-midpoint correction, 1917×1065: the evidence card now spans the same full vertical range as the right-side title-plus-assessment composition. Their top and bottom edges align, the assessment card no longer extends beyond the left visual, and the proof rail starts after the complete hero without overlaying either column.
Hero equal-midpoint correction, 390×844: the desktop-only grid alignment rule does not affect the mobile stack; title, calls to action, evidence visual, and assessment form remain readable in their existing order.
Viewport-centered hero correction, 1917×1065: the landing hero uses the available viewport beneath the navbar, with the left evidence card and the complete right-side title, actions, and assessment form sharing the same vertical center. The evidence card, title, and form retain their existing component styling and no following section overlays the hero.
Viewport-centered hero correction, 390×844: the desktop flex layout does not apply to mobile; the evidence visual, title, actions, and assessment form retain their existing readable stacked layout.
Hero action-gap and header-prominence QA, 1917×1065: the assessment console begins below the hero actions with a deliberate clear gap and no overlap. The desktop brand mark, navigation text, and theme control are visibly larger while preserving the existing color system and balanced header spacing.
Hero action-gap and header-prominence QA, 390×844: the desktop-only sizing rules do not affect mobile. The compact brand, theme control, menu trigger, evidence visual, hero actions, and stacked assessment console remain readable and without overflow.
Expanded assessment-panel QA, 1917×1065: the assessment panel now begins well below the hero actions, giving a clean visual gap. Its larger desktop footprint reaches into the former blank lower area, and its unchanged form contents are vertically centered inside the panel rather than leaving an empty bottom region.
Expanded assessment-panel QA, 390×844: the desktop-only expanded-panel rule does not affect mobile; the existing compact assessment form stays readable and retains its original stacking behavior.
Viewport-fit hero QA, 1917×1065: the evidence card is capped beneath the navbar and fully visible inside the hero. Its radar and caption are distributed between the card’s top and bottom. The right panel begins with Start a review near its top edge instead of after a large blank region, and the complete two-column composition is visible without clipping.
Viewport-fit hero QA, 390×844: the new viewport-fit rules do not affect mobile; the compact header, evidence visual, hero copy, actions, and assessment form remain readable in their existing stacked sequence.
Root hero-layout QA, 1440×900: the headline clears the navbar, the evidence card is naturally sized without stretching, and the right title-and-form stack has no dead lower panel whitespace. Both columns are vertically centered and fully visible within the hero viewport.
Root hero-layout QA, 1280×800: the complete evidence card, headline, calls to action, authorization form, and Run secure assessment action remain visible within the first viewport. The two natural-height columns share the same vertical midpoint, with no clipping or stretching.
Root hero-layout QA, 390×844: the desktop-only root flex rules do not affect the existing compact mobile hero, which remains readable in its stacked arrangement.
Hero top-alignment QA, 1440×900: the evidence card and the right-side headline now start at the same visual top edge directly beneath the navbar. The hero begins closer to the header, and the form remains fully visible with a clear action-to-panel gap.
Hero top-alignment QA, 390×844: the desktop-only top-alignment adjustment does not change the compact mobile sequence or its readable form controls.
Equal-height hero QA, 1440×900: the evidence card now matches the natural height of the complete right-side headline, action, and assessment stack. The radar is vertically centered in the taller card, the caption remains at the lower edge, and the full two-column row is centered in the remaining viewport beneath the navbar.
Equal-height hero QA, 390×844: the inner desktop row is not applied to mobile, preserving the readable evidence-card, headline, action, and assessment-form stack.
Final hero alignment QA, 1440×900: the evidence-card and headline tops are now visually aligned. The complete equal-height row is shifted slightly upward beneath the header without changing its component sizing, content, colors, or panel treatment.
Final hero alignment QA, 390×844: the desktop-only row placement and evidence-card nudge do not affect the existing mobile stack.
Radar-sweep centering QA, 1440×900: the moving green sweep is lowered around the stationary shield so its animated center is visually balanced rather than sitting high in the radar field.
Radar-sweep centering QA, 390×844: the same lowered sweep remains centered around the shield in the compact evidence card. Reduced-motion preference continues to stop the decorative rotation.
Whole-radar centering QA, 1440×900: the circular radar, shield, and moving green sweep now sit at the vertical midpoint of the Public Evidence Field card, while the existing lower-right caption remains unchanged.
Whole-radar centering QA, 390×844: the compact evidence card preserves its balanced radar placement and caption; no mobile sizing, text, color, or interaction changed.
Saved-report refinement QA, 1440×900 light view: the five top actions have a clear visual gap from the compact icon rail, the page has a restrained pale teal backdrop, and the report/comparison cards remain lighter surfaces. The C grade indicator is larger with an amber grade-matched ring; each comparison metric has a distinct muted teal-green left accent.
Saved-report refinement QA, 390×844 light view: the mobile header and all five actions remain fully usable in their existing compact grid. The larger amber C indicator, light card contrast, and report reading layout remain legible with no horizontal overflow.
Sidebar follow-up finding, 2026-08-28: the shadcn Sidebar renders both a desktop gap and a fixed inner surface using `bg-sidebar`, while report-reading mode is inferred from the `report-reading-surface` class. The visible pale output means the reliable repair must set the component-owned rendered surface directly rather than depend on route-level cascade overrides.
